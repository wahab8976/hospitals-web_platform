import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard-Hospital",
  description: "A Dashboard to Manage your Patient base",
};

const page = () => {
  return <div>This is Dashboard Page</div>;
};

export default page;
